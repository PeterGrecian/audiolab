aim:

switch between sources and destinations without plugging things in and
out

outputs:

headphones, speakers

inputs:

radio, eeek, phone

have a number of DPST switches minature slider

have a board for mounthing on underneeth

have some audio sockets. Maybe I only need this for the outputs. The
inputs can be flying leads (strain releaved x 3)
